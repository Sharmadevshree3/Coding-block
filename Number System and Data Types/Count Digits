#include<iostream>
using namespace std;

int main() {
    long long n;
    int d;
    cin >> n;
    cin >> d;

    int count = 0;

    while(n > 0) {
        if(n % 10 == d) {
            count++;
        }
        n = n / 10;
    }

    cout << count;
    return 0;
}
